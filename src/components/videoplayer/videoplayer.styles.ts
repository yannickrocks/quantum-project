import { stylesheet } from 'typestyle';

export default stylesheet({
  video: {
    display: 'flex',
    justifyContent: 'center',
    marginTop: '-60px',
  },
  iframe: {
    border: 'none',
    marginTop: '30px',
    marginBottom: '30px',
  },
});
